# Fitness Record v1.0 Final Candidate

Run with `python3 -m http.server 8000` and open `http://localhost:8000`.

Changes: local-date fix, reference photo popup, unified Plan category flow, Default Warm-up removed, Library Delete, workout name snapshots.
